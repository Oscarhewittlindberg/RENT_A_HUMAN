<p id="notice"><%= notice %></p>
    <%= simple_form_for(@booking) do |f| %>

      <div class="form-inputs">
        <%= f.input :name %>
        <%= f.input :description %>
        <%= f.input :date %>
        <%= f.input :duration %>
      </div>

      <div class="form-actions">
        <%= f.submit "Book", class: "btn btn-primary my-2 my-sm-0 ml-2"%>
      </div>
  <% end %>

